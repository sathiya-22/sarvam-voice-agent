# 🤖 Sarvam Voice Agent
Full voice loop: record → STT → Sarvam-M → TTS → playback in Indian languages.

```bash
pip install -r requirements.txt && cp .env.example .env
python agent.py hi-IN   # Hindi
python agent.py ta-IN   # Tamil
```
